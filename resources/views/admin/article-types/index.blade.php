@extends('admin.layouts.app')

@section('content')
    <div class="flex flex-col gap-6 px-4 sm:px-0"><div class="flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between"><div><a href="{{ route('admin.writing-rules.index') }}" class="text-sm font-semibold text-blue-600 hover:text-blue-800">← 返回写作规则</a><h1 class="mt-3 text-2xl font-bold text-gray-900">文章类型</h1><p class="mt-1 text-sm text-gray-600">定义文章用途、默认篇幅和推荐结构，写作规则可在此基础上继续细化。</p></div><a href="{{ route('admin.article-types.create') }}" class="rounded-md bg-blue-600 px-4 py-2 text-sm font-semibold text-white hover:bg-blue-700">新建文章类型</a></div>
        <div class="grid grid-cols-1 gap-4 md:grid-cols-2 xl:grid-cols-3">@forelse ($articleTypes as $type)<article class="flex flex-col rounded-lg border border-gray-200 bg-white p-5 shadow-sm"><div class="flex items-start justify-between gap-3"><div><h2 class="font-semibold text-gray-900">{{ $type->name }}</h2><p class="mt-1 text-xs text-gray-500">{{ $type->code }}</p></div><span class="rounded-full px-2 py-1 text-xs font-semibold {{ $type->is_system ? 'bg-violet-100 text-violet-700' : 'bg-gray-100 text-gray-600' }}">{{ $type->is_system ? '系统预设' : '自定义' }}</span></div><p class="mt-4 grow text-sm leading-6 text-gray-600">{{ $type->description ?: '暂无说明' }}</p><div class="mt-4 flex items-center justify-between border-t border-gray-100 pt-4 text-xs text-gray-500"><span>{{ $type->writing_rules_count }} 条规则 · {{ $type->is_active ? '启用' : '停用' }}</span>@unless ($type->is_system)<a href="{{ route('admin.article-types.edit', $type) }}" class="font-semibold text-blue-600">编辑</a>@endunless</div></article>@empty<div class="md:col-span-2 xl:col-span-3 rounded-lg border border-dashed border-gray-300 p-12 text-center text-sm text-gray-500">暂无文章类型。</div>@endforelse</div>
    </div>
@endsection
